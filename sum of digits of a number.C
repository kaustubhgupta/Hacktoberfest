#include<stdio.h>
void main()
{
  int a,b,sum=0;
  printf("Enter a no:");
  scanf("%d", &a);
  printf("Enter another no:");
  scanf("%d", &b);
  sum=a+b;
  printf("Sum of digits is %d",sum);
}
