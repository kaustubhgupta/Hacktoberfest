#include<stdio.h>
#include<conio.h>

int main()
{
    int num,factors,i;
    printf("Enter a no:");
    scanf("%d", &num);
    for(i=1;i<=a;i++)    /* This function shows factorization */
    {
      if(num%i=0)
      {
         factor=num/i;
         printf("%d\n",factor);
      }

    }
   return 0;
 }
