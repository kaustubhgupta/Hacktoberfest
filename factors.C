#include<stdio.h>
#include<conio.h>
void main()
{
  int a b;
  pritf("Enter a no:");
  scan("%d", &a);
  for(int i=1;i<=a;i++)
  {
    if(a%i=0)
    b=a//i;
    printf("%d\n",b)

  }
   getch();
   }
